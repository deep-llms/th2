# manager: conda
# fresh: true
# python: 3.11
# Fresh-install fallback matching the previous sparse_emb runner specification.
# Existing-machine setup uses an offline conda clone to preserve exact versions.
# Choose/verify the runner's hardware-compatible torch; do not force dev cu118.
torch
transformers==5.9.0
datasets==4.8.5
accelerate==1.13.0
scipy
wandb
pyarrow
huggingface_hub
openai
entmax
