# Seed-17 Stage-1 reader diagnostic

User authorized sequential training of the next memory arms on 2026-09-13.
This run does not start Stage-2 continuation, rebuild tables, change the
compiler, or access locked validation data.

## Locked settings

- Common checkpoint: `ccm_pilot_seed17_20260913_a01/common/checkpoint-15259`.
- Its model SHA256: `844c0b0320e88c446f19d2ebd028857ba5b8a4a4e61d2ced740c017eaf8249cf`.
- Original completed 1B-token compiler tables, not dev-benchmark tables.
- Backbone seed 17; paired reader seed 100017; data-order seed 1017;
  Grad-table seed 300017; existing shuffle seed 200017.
- Train order: Contextual → Isolated → Shuffled → Shallow → Delta → Grad.
- Every arm starts independently from the same common checkpoint, with fresh
  paired reader initialization and a fresh optimizer. Backbone is frozen.
- 977 updates / 256114688 adaptation input tokens per arm, global token batch
  262144, bf16 parameters, fp32 Adam masters/moments, reader peak LR 5e-4,
  Grad table peak LR 2.5e-3, 49-step warmup, cosine to 10% of peak.
- Eight-rank torchrun, 8 segments per device microbatch, loss chunk 1024,
  non-reentrant activation checkpointing: same settings as the B200 smoke.
  No Accelerate configuration is consumed by this trainer.

## Sequence and safety

Entry: `bash scripts/launch_pilot_stage1.sh`; persistent session
`ccm_stage1_20260913_a01`. Workflow: `scripts/pilot_stage1.py`.
Remote output root:
`/mnt/local/_outputs/deep-llms_th2/ccm_stage1_seed17_20260913_a01`.
Sibling log: the same path plus `.handoff.log`.

1. Validate local-only common checkpoint, corpus, coverage, and all five
   table identities while existing burns remain active.
2. Require old workflow success, set its `STOP_IDLE_WATCH`, and wait for its
   exact observer process to exit. Do not signal that process or its tmux.
3. Verify original runner-burn ownership; signal only verified worker pidfds.
   Wait 30s/check all GPUs free, then wait 30s/check again.
4. Train each arm on all eight GPUs, sequentially. After each successful exit,
   wait 30s/check free, validate checkpoint/log/optimizer counters and schedule,
   byte-identical frozen backbone, unchanged frozen tables, changed Grad table,
   and paired reader initialization before advancing.
5. Run communicating original burns on GPUs 1–7. Evaluate Base, then each of
   the six adapted checkpoints sequentially on GPU 0, on full 20M-token dev.
   All evaluations use Contextual's variance bins for comparable diagnostics.
   Validate per-segment IDs/counts, checksums, NLL summaries and diagnostic bins.
6. Reclaim only the verified seven-GPU burn group, wait/check all GPUs free,
   and restore the original all-eight-GPU communicating burn.
7. Compute Contextual-minus-Isolated and Contextual-minus-Shuffled overall
   development NLL contrasts: 10000 document-cluster bootstrap replicates,
   `doc_id` / `independent`, per clarification v2. Validate the complete panel.
8. Write success only after all stages pass. Continue an idle-burn observer;
   a future authorized job must set this new workflow's `STOP_IDLE_WATCH`
   and verify observer exit before reclaiming GPUs.

There is no automatic training retry, deletion or checkpoint resume. On a
failure, preserve evidence and restore burns only if the known job has ended
and GPU ownership/free checks permit it. A live child or unknown GPU process
is never killed by failure recovery. Stage-1 Base is evaluated without any
adaptation; Stage-2, if later authorized, must initialize fresh modules again.

## Local verification

79 tests passed in `temp/stage1_prelaunch_tests_20260913.log`; shell syntax and
Python compilation passed. Existing full-size, eight-B200 Stage-1 Contextual
and Grad smoke results are documented in `B200_SMOKE_20260913.md`.
No `ccm/` file changed: the existing scientific artifact source hash remains
`055f0518853e76487a4e2f31b81f1441113d4fceab322b5e211359a7d58f2fa9`.
