# Seed-17 Stage-2 matched continuation

Authorized on 2026-09-13 after Stage-1 training, seven dev evaluations and
the primary comparisons passed their artifact checks. This is the planned
next phase, not another seed or a repeat of frozen-backbone adaptation.

## Locked experiment

- Train sequentially: Base-Continue → Contextual → Isolated → Shuffled → Grad.
- Every arm independently loads the original common `checkpoint-15259`
  (model SHA256 `844c0b0320e88c446f19d2ebd028857ba5b8a4a4e61d2ced740c017eaf8249cf`).
- Same backbone seed 17, paired reader seed 100017, data-order seed 1017,
  Grad-table seed 300017. Never load Stage-1 adapted modules or optimizer state.
- Fresh AdamW with fp32 master weights/moments; backbone trainable; compiled
  tables frozen; Grad table trainable. No online writing or table rebuilding.
- Exactly 3815 steps / 1000079360 non-padding input tokens on `D_continue`.
  Global batch 262144 tokens, same immutable corpus and paired batch order.
- Backbone peak LR 1.5e-4, 77 warmup updates, cosine to 1.5e-5. Reader and
  Grad-table LR 5× backbone; weight decay follows the frozen protocol.
- Eight-GPU torchrun, bf16, microbatch 8 segments/device, loss chunks 1024,
  non-reentrant activation checkpointing; no Accelerate config is consumed.
- Save checkpoints at 1000, 2000, 3000 and 3815. Fresh outputs only; no resume,
  automatic retry, old-output deletion, locked-val evaluation or other seeds.

Delta is excluded by clarification v2 §6: Stage-1 overall dev NLL
3.240717930 exceeds Contextual's 3.240683974. Failing that necessary safeguard
is sufficient for exclusion; no bootstrap inclusion claim is made. Shallow is
Stage-1-only under the frozen protocol.

## Handoff and verification

Entry: `bash scripts/launch_pilot_stage2.sh`; workflow:
`scripts/pilot_stage2.py`; artifact gates: `scripts/validate_stage2.py`.
Environment: `train_env`; node: `thiennh-p6-8mgy-worker-0`.
Session: `ccm_stage2_20260913_a01`.
Output: `/mnt/local/_outputs/deep-llms_th2/ccm_stage2_seed17_20260913_a01`.
Sibling handoff log: that path plus `.handoff.log`.

1. Check node, environment, original burn identity, fresh paths, disk space,
   Stage-1 completion/results, common checkpoint, corpus/vocabulary and tables
   while the existing burns remain active.
2. Disarm the Stage-1 idle observer through its `STOP_IDLE_WATCH` file and
   wait for the exact observer process to exit voluntarily. Never signal it.
3. Stop only verified original burn-worker pidfds, never a name, launcher,
   PID 1 or process group. Wait 30s/check all GPUs free twice before training.
4. Train the five arms sequentially on all eight GPUs. After each exit zero,
   wait 30s/check free and validate exact budgets/LR, model/checkpoint hashes,
   updated backbone blocks, fresh paired modules, unchanged frozen tables,
   optimizer parameter membership, fp32 masters/moments and reset counters.
5. Evaluate all five final checkpoints on full 20M-token dev, GPU 0 sequentially,
   with communicating burns on GPUs 1–7. Use Contextual's common-table variance
   bins for every arm. Check aligned records, totals, bins and hashes.
6. Reclaim the verified seven-GPU burn group, wait/check, restore all-eight-GPU
   original `/tmp/llm_pretrain_burn.py` burns. Compute document-bootstrap
   Contextual-minus-Isolated and Contextual-minus-Shuffled primary contrasts;
   Contextual-minus-Base and Contextual-minus-Grad are additional comparisons.
   All use `doc_id`, `independent`, 10000 replicates, seed 20260913.
7. Validate the panel and publish `complete.json` only after successful checks.
   Scientific losses need not favor Contextual for execution to be successful.
   The observer keeps checking burns. A future GPU job must disarm THIS new
   root's `STOP_IDLE_WATCH` and verify its observer has exited before reclaim.

Failure stops the sequence, retains evidence and never retries training. Burn
recovery refuses a live child or unknown GPU workload. Status is written every
minute during a child job; read-only #2 exports do not relaunch training.
Dropbox flattens source parents: status is inside
`/mnt_local__outputs_deep-llms_th2_ccm_stage2_seed17_20260913_a01/`, and the
sibling handoff log is inside `/mnt_local__outputs_deep-llms_th2/`.

The production research code under `ccm/` is unchanged. Existing eight-B200
Stage-2 Contextual/Grad smoke evidence is in `B200_SMOKE_20260913.md`; new
handoff tests exercise command parsing, order, failures, observer disarming,
the LR contract and optimizer-layout validation without touching GPUs.

Prelaunch checks: canonical dev suite 86 passed (24.06s); execution checkout
suite 39 passed (19.91s), excluding its dev-only Dropbox-monitor test because
the Dropbox client is intentionally not deployed. Shell syntax and compilation
passed. Execution research-core hash remains
`055f0518853e76487a4e2f31b81f1441113d4fceab322b5e211359a7d58f2fa9`.
The canonical core differs only by trailing blank lines in three files; no
such whitespace or other core changes are deployed for this continuation.
